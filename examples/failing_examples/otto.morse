#
# descrizione di uno sketch a forma di otto
# (questo non e' compatibile con le restrizioni
# del lavoro, ma per la costruzione dello sketch
# e' OK
#
morse {
    A[l,0];  # l: left (orientazione verso sinistra), d = 0
    X[d,1];
    V;
}
