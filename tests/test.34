#testname:unknot the gordian knot (takes a few seconds)
#
# unknot the gordian knot of Haken
#
echo "unknotting the gordian knot..."

./contour --out fg examples/knots/hardunknot_gordian.dtcode -q

echo "unknotting the joel hass knot..."
./contour --out fg examples/knots/hardunknot_joel_hass.knot -q
